import React from "react";

const CardTitle = ({ children }) => {
  // return <div className="text-[#ebd26e]">{children}</div>;
  return <div className="text-[#fff]">{children}</div>;
};

export default CardTitle;
